[project]
name = "pytils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "exchangelib>=5.5.1",
    "msal>=1.34.0",
    "pandas>=2.3.2",
    "python-dotenv>=1.1.1",
]

[dependency-groups]
dev = [
    "ipython>=9.5.0",
    "notebook>=7.4.7",
]
